{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "news.published",
  "description": "Emitted when a news article is transitioned from draft to published status",
  "type": "object",
  "properties": {
    "event_id": { "type": "string", "format": "uuid" },
    "event_name": { "type": "string", "const": "news.published" },
    "occurred_at": { "type": "string", "format": "date-time" },
    "actor_id": { "type": "string", "format": "uuid" },
    "correlation_id": { "type": "string", "format": "uuid" },
    "causation_id": { "type": "string", "format": "uuid" },
    "payload": {
      "type": "object",
      "properties": {
        "news_id": { "type": "string", "format": "uuid" },
        "author_id": { "type": "string", "format": "uuid" },
        "title": { "type": "string" },
        "published_at": { "type": "string", "format": "date-time" }
      },
      "required": ["news_id", "author_id", "title", "published_at"],
      "additionalProperties": false
    }
  },
  "required": ["event_id", "event_name", "occurred_at", "actor_id", "correlation_id", "causation_id", "payload"]
}
